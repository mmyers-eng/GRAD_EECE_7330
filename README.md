This project explores embedded machine learning.


Docker installation:
Install Docker:
https://docs.docker.com/engine/install/ubuntu/


Building the docker container
cd ./docker
docker build -t stm32-env .

Run the docker container mounting this repo as "workspace"
cd ..
Build
